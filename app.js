(() => {
  const catalog = window.CATALOG_DATA;
  if (!catalog?.works?.length) return;

  const catalogPatches = {
    1: {
      title: "Herbstfarben an der Ostsee (?)",
      artist: "Unbekannter Künstler; Signatur undeutlich (möglicherweise „W. Wopf“ oder ähnlich)",
      technique: "vermutlich Öl auf Hartfaser-/Malplatte",
      dating: "wahrscheinlich 2. Hälfte 20. Jahrhundert",
      dimensions: "66 × 54 cm",
      framedDimensions: "73,5 × 61,5 cm",
      value: "ca. 150–300 €",
      status: "katalogisiert",
      catalogText: "G-001 – Herbstfarben an der Ostsee (?)\n\nUnbekannter Künstler; die Signatur ist auf den vorhandenen Fotos nicht sicher lesbar und wurde im früheren Chat vorsichtig als möglicherweise „W. Wopf“ oder ähnlich diskutiert. Rückseitig ist eine rote Beschriftung vorhanden, deren erste Zeile wahrscheinlich „Herbstfarben an d. Ostsee“ lautet. Motiv: herbstliche Küsten-/Ostseelandschaft mit Wasser und kleinen Segelbooten. Technik vermutlich Öl auf Hartfaser- oder Malplatte. Bildmaß 66 × 54 cm; Rahmenmaß 73,5 × 61,5 cm. Datierung wahrscheinlich 2. Hälfte des 20. Jahrhunderts. Zustand nach den Fotos insgesamt ordentlich, mit altersüblichen Spuren an Bild und Rahmen. Solange die Künstleridentifikation nicht gesichert ist, bleibt die Einordnung als dekoratives Originalgemälde des 20. Jahrhunderts bestehen. Vorläufiger Marktwert ca. 150–300 €. Eine sichere Entzifferung der Signatur oder der rückseitigen Beschriftung könnte die Bewertung verändern."
    },
    73: {
      title: "Stillleben mit Melone – Replik",
      artist: "nach Paula Modersohn-Becker (1876–1907)",
      technique: "Dietz-Replik / hochwertige Farbreproduktion, wohl mehrfarbiger Siebdruck",
      dating: "2. Hälfte 20. Jahrhundert; Vorlage von 1905",
      dimensions: "",
      framedDimensions: "62,5 × 77,5 cm",
      value: "ca. 40–100 €",
      status: "katalogisiert",
      catalogText: "G-073 – Paula Modersohn-Becker, „Stillleben mit Melone“ – Dietz-Replik\n\nGerahmte Replik nach Paula Modersohn-Becker. Das rückseitige Blatt bezeichnet das Motiv als „Stillleben mit Melone“ (1905) und nennt Carl Schünemann Bremen / Verlag der Dietz Offizin. Laut Rückseitenblatt wurde diese Replik der Dietz Offizin in einer limitierten Auflage von 300 Exemplaren hergestellt; als Standort des Originals wird das Wallraf-Richartz-Museum in Köln genannt. Es handelt sich damit nicht um ein Originalgemälde Modersohn-Beckers, sondern um eine hochwertige, limitierte Reproduktion. Die Dietz-Offizin arbeitete bei solchen Repliken mit aufwendigen mehrfarbigen Druckverfahren und strukturierter Oberflächenwirkung; für dieses Exemplar ist die genaue technische Ausführung ohne Öffnung der Rahmung nicht abschließend festzulegen. Außenmaß mit Rahmen 62,5 × 77,5 cm. Nach den vorliegenden Fotos insgesamt dekorativ und ordentlich erhalten. Vergleichbare ältere Dietz-Repliken erzielen im Auktionsmarkt meist eher niedrige zweistellige Beträge; für dieses gerahmte, limitierte Exemplar ist ein realistischer Privatverkaufswert von etwa 40–100 € anzusetzen."
    },
    74: {
      title: "Jagdreiter mit Hundemeute",
      artist: "Künstler nicht sicher identifiziert; handschriftliche Rand-Signatur vorhanden",
      technique: "Druck / Reproduktion hinter Glas",
      dating: "vermutlich 20. Jahrhundert; Motiv nach älterer englischer Jagdgrafik",
      dimensions: "",
      framedDimensions: "72 × 77,5 cm",
      value: "ca. 60–150 €",
      status: "katalogisiert",
      catalogText: "G-074 – Jagdreiter mit Hundemeute\n\nGroßformatige gerahmte Jagddarstellung mit einem Reiter auf hellem Pferd und einer größeren Hundemeute. Nach dem Fotobefund handelt es sich um einen Druck bzw. eine Reproduktion hinter Glas, nicht um ein Gemälde. Am Rand befindet sich eine handschriftliche Signatur, die auf den vorhandenen Aufnahmen jedoch nicht sicher gelesen und deshalb keinem Künstler belastbar zugeordnet werden kann. Rückseitig bzw. an der Rahmung findet sich ein Hinweis auf „Walnut Lane Art Shop, Germantown, Philadelphia“, der als Provenienz- bzw. Rahmerhinweis dokumentiert wird. Außenmaß mit Rahmen 72 × 77,5 cm. Stilistisch gehört das Blatt in die Tradition englischer bzw. angloamerikanisch gesammelter Jagd- und Pferdegrafik; die konkrete Ausgabe und das genaue Alter bleiben ohne sichere Signaturlesung offen. Zustand nach den Fotos insgesamt ordentlich und dekorativ, mit altersüblichen Rahmungsspuren. Solange Künstler und Druckausgabe nicht geklärt sind, liegt ein realistischer Privatverkaufswert ungefähr bei 60–150 €. Bei erfolgreicher Identifizierung der Rand-Signatur oder einer nachweislich frühen Druckausgabe wäre eine Neubewertung sinnvoll."
    },
    75: {
      title: "The Royal Mail Coach",
      artist: "nach John Frederick Herring Sr. (1795–1865)",
      technique: "kolorierte Farbgrafik / Reproduktion hinter Glas",
      dating: "wahrscheinlich 20. Jahrhundert; nach einer Vorlage des 19. Jahrhunderts",
      dimensions: "",
      framedDimensions: "68,5 × 61,5 cm",
      value: "ca. 50–120 €",
      status: "katalogisiert",
      catalogText: "G-075 – THE ROYAL MAIL COACH\n\nGerahmte historische Kutschendarstellung nach John Frederick Herring Sr. Auf dem Blatt sind der Titel „THE ROYAL MAIL COACH“ und die Bezeichnung „PAINTED BY J. F. HERRING.“ sichtbar. Herring war ein britischer Pferde-, Tier- und Sportmaler; seine Kutschenmotive wurden im 19. Jahrhundert vielfach druckgrafisch verbreitet. Die Darstellung lässt sich sehr wahrscheinlich mit „The Royal Mail Coach“ aus der Folge „Herring's Sketches on the Road“ verbinden. Nach Größe und Fotobefund ist für das vorliegende Stück eine spätere, verkleinerte Farbreproduktion bzw. Farbgrafik wahrscheinlicher als ein originaler großformatiger Abzug des 19. Jahrhunderts. Die heutige Rahmung trägt rückseitig den Stempel „SPEEDY – PICTURE FRAMING – 1703 TELEPHONE RD – WA 3-4248 – HOUSTON, TEXAS 77023“; dieselbe Adresse und Telefonnummer sind für die 1960er Jahre nachweisbar, sodass die Rahmung plausibel in diese Zeit gehört. Außenmaß mit Rahmen 68,5 × 61,5 cm. Zustand nach den vorhandenen Fotos insgesamt ordentlich und dekorativ. Eine eigenhändige Arbeit Herrings liegt nicht vor. Für die wahrscheinlich spätere Reproduktion ist ein realistischer Privatverkaufswert von etwa 50–120 € anzusetzen; ein früher historischer Abzug müsste nach Papier, Drucktechnik und Randangaben separat geprüft und deutlich höher bewertet werden."
    }
  };

  for (const [sequence, patch] of Object.entries(catalogPatches)) {
    const work = catalog.works.find((entry) => entry.sequence === Number(sequence));
    if (work) Object.assign(work, patch);
  }
  catalog.stats.catalogued = catalog.works.filter((work) => work.status === "katalogisiert").length;

  const grid = document.querySelector("#art-grid");
  const search = document.querySelector("#search");
  const statusFilter = document.querySelector("#status-filter");
  const sortOrder = document.querySelector("#sort-order");
  const resultLine = document.querySelector("#result-line");
  const emptyState = document.querySelector("#empty-state");
  const dialog = document.querySelector("#art-dialog");
  const lightbox = document.querySelector("#lightbox");
  let activeWork = null;
  let activeImageIndex = 0;

  const normalized = (value) =>
    String(value ?? "")
      .normalize("NFD")
      .replace(/[\u0300-\u036f]/g, "")
      .toLowerCase();

  const escapeHtml = (value) =>
    String(value ?? "")
      .replaceAll("&", "&amp;")
      .replaceAll("<", "&lt;")
      .replaceAll(">", "&gt;")
      .replaceAll('"', "&quot;")
      .replaceAll("'", "&#039;");

  const displayTitle = (work) =>
    work.title && work.title !== work.id ? work.title : "Ohne gesicherten Titel";

  document.querySelector("#stat-works").textContent = catalog.stats.works;
  document.querySelector("#stat-photos").textContent = catalog.stats.photos;
  document.querySelector("#stat-catalogued").textContent = catalog.stats.catalogued;

  const heroWorks = catalog.works.filter((work) => work.images.length);
  const heroSelection = [heroWorks[39] ?? heroWorks[0], heroWorks[70] ?? heroWorks[1]].filter(Boolean);
  document.querySelector("#hero-art").innerHTML = heroSelection
    .map(
      (work) => `<div class="hero-image"><img src="${escapeHtml(work.images[0].src)}" alt="" /></div>`,
    )
    .join("");

  function searchText(work) {
    return normalized(
      [
        work.id,
        work.title,
        work.artist,
        work.technique,
        work.dating,
        work.dimensions,
        work.userNotes?.join(" "),
        work.catalogText,
      ].join(" "),
    );
  }

  function visibleWorks() {
    const query = normalized(search.value.trim());
    const status = statusFilter.value;
    const works = catalog.works.filter(
      (work) =>
        (status === "all" || work.status === status) &&
        (!query || searchText(work).includes(query)),
    );

    return works.sort((a, b) => {
      if (sortOrder.value === "title")
        return displayTitle(a).localeCompare(displayTitle(b), "de");
      if (sortOrder.value === "artist")
        return a.artist.localeCompare(b.artist, "de");
      return a.sequence - b.sequence;
    });
  }

  function cardMarkup(work) {
    const image = work.images[0];
    const subtitle = [work.artist, work.technique].filter(Boolean).join(" · ");
    return `
      <article class="art-card">
        <button type="button" data-work-sequence="${work.sequence}" aria-label="${escapeHtml(displayTitle(work))} öffnen">
          <div class="card-image">
            <img src="${escapeHtml(image.src)}" alt="${escapeHtml(displayTitle(work))}" loading="lazy" decoding="async" />
            ${work.images.length > 1 ? `<span class="photo-count">${work.images.length} Fotos</span>` : ""}
            ${work.status === "ausstehend" ? '<span class="status-pill">Offen</span>' : ""}
          </div>
          <div class="card-copy">
            <div class="card-kicker"><span>${escapeHtml(work.id)}</span><span>${escapeHtml(work.dating || "")}</span></div>
            <h3>${escapeHtml(displayTitle(work))}</h3>
            <p>${escapeHtml(subtitle || "Zuordnung in Prüfung")}</p>
          </div>
        </button>
      </article>`;
  }

  function render() {
    const works = visibleWorks();
    grid.innerHTML = works.map(cardMarkup).join("");
    resultLine.textContent = `${works.length} von ${catalog.works.length} Werken`;
    emptyState.hidden = works.length > 0;

    grid.querySelectorAll("[data-work-sequence]").forEach((button) => {
      button.addEventListener("click", () => {
        const work = catalog.works.find(
          (entry) => entry.sequence === Number(button.dataset.workSequence),
        );
        openWork(work);
      });
    });
  }

  function metadataMarkup(work) {
    const rows = [
      ["Technik", work.technique],
      ["Datierung", work.dating],
      ["Maße", work.dimensions],
      ["Mit Rahmen", work.framedDimensions],
      ["Wertspanne", work.value],
      ["Status", work.status === "katalogisiert" ? "Vorläufig katalogisiert" : "Katalogisierung ausstehend"],
    ].filter(([, value]) => value);

    return rows
      .map(([label, value]) => `<div><dt>${escapeHtml(label)}</dt><dd>${escapeHtml(value)}</dd></div>`)
      .join("");
  }

  function selectDialogImage(index) {
    activeImageIndex = index;
    const image = activeWork.images[index];
    const main = document.querySelector("#dialog-main-image");
    main.src = image.src;
    main.alt = `${displayTitle(activeWork)}, Aufnahme ${index + 1} von ${activeWork.images.length}`;
    document.querySelectorAll("#thumbnail-strip button").forEach((button, buttonIndex) => {
      button.classList.toggle("active", buttonIndex === index);
    });
  }

  function openWork(work) {
    if (!work) return;
    activeWork = work;
    activeImageIndex = 0;
    document.querySelector("#dialog-id").textContent = work.id;
    document.querySelector("#dialog-title").textContent = displayTitle(work);
    document.querySelector("#dialog-artist").textContent = work.artist;
    document.querySelector("#dialog-metadata").innerHTML = metadataMarkup(work);
    document.querySelector("#dialog-notes").innerHTML = (work.userNotes?.length
      ? work.userNotes
      : ["Keine zusätzlichen Maß- oder Provenienzangaben erfasst."]
    )
      .map((note) => `<p>${escapeHtml(note)}</p>`)
      .join("");
    document.querySelector("#dialog-catalog").textContent =
      work.catalogText ||
      "Für dieses Werk liegt im geteilten Chat noch kein abgeschlossener Katalogtext vor.";
    document.querySelector("#catalog-details").open = false;
    document.querySelector("#thumbnail-strip").innerHTML = work.images
      .map(
        (image, index) => `
          <button type="button" aria-label="Aufnahme ${index + 1} anzeigen">
            <img src="${escapeHtml(image.src)}" alt="" loading="lazy" />
          </button>`,
      )
      .join("");
    document.querySelectorAll("#thumbnail-strip button").forEach((button, index) => {
      button.addEventListener("click", () => selectDialogImage(index));
    });
    selectDialogImage(0);
    dialog.showModal();
    history.replaceState(null, "", `#werk-${work.sequence}`);
  }

  function closeDialog(target = dialog) {
    target.close();
    if (target === dialog) history.replaceState(null, "", "#sammlung");
  }

  [search, statusFilter, sortOrder].forEach((control) =>
    control.addEventListener("input", render),
  );
  document.querySelector("#reset-filters").addEventListener("click", () => {
    search.value = "";
    statusFilter.value = "all";
    sortOrder.value = "sequence";
    render();
  });
  document.querySelector("#dialog-close").addEventListener("click", () => closeDialog());
  dialog.addEventListener("click", (event) => {
    if (event.target === dialog) closeDialog();
  });
  document.querySelector("#main-image-button").addEventListener("click", () => {
    const image = activeWork.images[activeImageIndex];
    document.querySelector("#lightbox-image").src = image.src;
    document.querySelector("#lightbox-image").alt = `${displayTitle(activeWork)}, vergrößerte Aufnahme`;
    lightbox.showModal();
  });
  document.querySelector("#lightbox-close").addEventListener("click", () => closeDialog(lightbox));
  lightbox.addEventListener("click", (event) => {
    if (event.target === lightbox) closeDialog(lightbox);
  });

  render();

  const match = location.hash.match(/^#werk-(\d+)$/);
  if (match) openWork(catalog.works.find((work) => work.sequence === Number(match[1])));
})();
